# widen-derive

Implementation of `#[derive(Widen)]` for [widen](https://docs.rs/widen).
Use the `widen` crate, which re-exports this macro with its default features.

Licensed under either MIT or Apache-2.0, at your option.
