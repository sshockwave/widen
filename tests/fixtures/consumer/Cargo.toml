[package]
name = "widen-test-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
# Exercise derive path resolution when the dependency is renamed.
sets = { package = "widen", path = "../../.." }
widen-test-traced = { path = "../traced" }
